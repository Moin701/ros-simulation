<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro" name="amr">

    <xacro:property name="M_PI" value="3.1415926535897931" />

    <xacro:arg name="robot_name" default="amr"/>
    <xacro:arg name="prefix" default=""/>
    <xacro:arg name="use_gazebo" default="false"/>
    <xacro:arg name="use_webots" default="false"/>

    <xacro:include filename="$(find amr_description)/urdf/mech/base.urdf.xacro"/>
    <xacro:include filename="$(find amr_description)/urdf/mech/wheel.urdf.xacro"/>
    <xacro:include filename="$(find amr_description)/urdf/sensors/camera.urdf.xacro"/>
    <xacro:include filename="$(find amr_description)/urdf/sensors/lidar.urdf.xacro"/>
    <xacro:include filename="$(find amr_description)/urdf/sensors/imu.urdf.xacro"/>

    <xacro:base prefix="$(arg prefix)"/>

    <xacro:wheel
      prefix="$(arg prefix)"
      side="front_left"
      x_reflect="1"
      y_reflect="1"
      contact_material="wheel_pos_45"/>

    <xacro:wheel
      prefix="$(arg prefix)"
      side="front_right"
      x_reflect="1"
      y_reflect="-1"
      contact_material="wheel_neg_45"/>

    <xacro:wheel
      prefix="$(arg prefix)"
      side="rear_left"
      x_reflect="-1"
      y_reflect="1"
      contact_material="wheel_neg_45"/>

    <xacro:wheel
      prefix="$(arg prefix)"
      side="rear_right"
      x_reflect="-1"
      y_reflect="-1"
      contact_material="wheel_pos_45"/>

    <xacro:camera
      prefix="$(arg prefix)"
      camera_name="cam_1"
      xyz_offset="0.105 0 0.05"
      rpy_offset="0 0 0"/>

    <!-- z=0.22: chassis.stl's own measured top is z=0.1905 (see
         base.urdf.xacro's bbox comment). The previous xyz_offset (0.0825)
         put the lidar's own mesh spanning roughly z=[0.062,0.101] -
         entirely inside the chassis's 0.0540..0.1905 height range (measured
         directly from lidar.stl + chassis.stl), so every one of its 360
         rays hit the inside of its own chassis shell within centimeters
         (well under minRange 0.1) and reported no return in every
         direction, regardless of the room's actual wall distances. This
         raises it clear of the chassis roof with a small margin. -->
    <xacro:lidar
      prefix="$(arg prefix)"
      parent="base_link"
      frame_id="laser_frame"
      xyz_offset="0 0 0.22"
      rpy_offset="0 0 0"
      mesh_xyz_offset="0 0 0"
      mesh_rpy_offset="${-M_PI/2} 0 0"
      min_range="0.05"
      topic_name="scan"/>

    <xacro:imu
      prefix="$(arg prefix)"
      frame_id="imu"
      xyz_offset="0 0 0.006"
      rpy_offset="0 0 0"
      update_rate="15.0"
      topic_name="imu/data"/>

    <xacro:include filename="$(find amr_description)/urdf/control/velocity_control.urdf.xacro" />
    <xacro:load_velocity_control use_gazebo="$(arg use_gazebo)"/>

    <xacro:include filename="$(find amr_description)/urdf/control/sim_control.urdf.xacro" />
    <xacro:load_sim_control
      robot_name="$(arg robot_name)"
      use_gazebo="$(arg use_gazebo)"/>

    <xacro:include filename="$(find amr_description)/urdf/control/ros2_control.urdf.xacro" />
    <xacro:amr_ros2_control
      prefix="$(arg prefix)"
      use_gazebo="$(arg use_gazebo)"
      use_webots="$(arg use_webots)"/>

</robot>
